lintering:
  parallel: true
  commands:
    rubocop:
      run: bundle exec rubocop
    fasterer:
      run: bundle exec fasterer

database_setup:
  parallel: true
  commands:
    create_database:
      run: rails db:create
    load_schema:
      run: rails db:schema:load
