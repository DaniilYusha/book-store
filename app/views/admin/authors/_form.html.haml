= semantic_form_for [:admin, @author], builder: ActiveAdmin::FormBuilder do |f|
  = render partial: 'admin/shared/errors', locals: { object: @service }
  = f.inputs 'Details' do
    = f.input :first_name
    = f.input :last_name
  = f.actions
