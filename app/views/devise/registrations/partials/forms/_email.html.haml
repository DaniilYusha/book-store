= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), method: :put, html: { id: 'edit-email' } ) do |f|
  = f.input :email
  .general-text-align
    = f.button :submit, t('buttons.save')
